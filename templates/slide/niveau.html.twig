{% extends "base.html.twig" %}
{% block body %}
<div class="container">
    <h3 class="text-center mt-4 mb-4"> choisir votre niveau d'apprentissage </h3>
    <div class="row">
      <div class="col-6">
          <a href="{{path('app_cours',{niveau:'FACILE',langue:id})}}" class="btn btn-primary">commencez par les bases si vous avez un niveau debutant</a>
      </div>
      <div class="col-6">
          <a href="{{path('app_cours',{niveau:'DIFFICILE',langue:id})}}" class="btn btn-primary">plongez dans le bain si vous ete deja un proessionnel</a>
      </div>
    </div>
</div>
{% endblock body %}