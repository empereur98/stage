{% extends "base.html.twig" %}
{% block body %}
{% for message in app.flashes('success') %}
<div class="alert alert-primary">
    {{ message }}
</div>
{%endfor%}
{{form_start(form)}}
{{form_end(form)}}
{% endblock body %}